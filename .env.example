# ODS-E Transform Harness live configuration
# Used by: tools/transform_harness.py
#
# Run:
#   PYTHONPATH=src/python python3 tools/transform_harness.py --mode mixed

# ------------------------------------------------------------
# Enphase demo/live (set full endpoint URL that returns payload
# compatible with EnphaseTransformer: end_at, wh_del, devices_reporting)
# ------------------------------------------------------------
# ODS_LIVE_ENPHASE_URL=https://api.enphaseenergy.com/...
# ODS_LIVE_ENPHASE_METHOD=GET
# ODS_LIVE_ENPHASE_HEADERS={"Authorization":"Bearer <token>"}
# ODS_LIVE_ENPHASE_TRANSFORM_KWARGS={"expected_devices":10}

# ------------------------------------------------------------
# SMA sandbox/live (set endpoint URL that returns normalized contract
# payload expected by SMATransformer)
# ------------------------------------------------------------
# ODS_LIVE_SMA_URL=https://sandbox.smaapis.de/monitoring/...
# ODS_LIVE_SMA_METHOD=GET
# ODS_LIVE_SMA_HEADERS={"Authorization":"Bearer <token>"}

# ------------------------------------------------------------
# Fronius local live
# Option A: set host only (harness will call GetPowerFlowRealtimeData.fcgi)
# ------------------------------------------------------------
# FRONIUS_HOST=192.168.1.50
#
# Option B: full URL override
# ODS_LIVE_FRONIUS_URL=http://192.168.1.50/solar_api/v1/GetPowerFlowRealtimeData.fcgi
# ODS_LIVE_FRONIUS_METHOD=GET

# ------------------------------------------------------------
# Generic live configuration pattern for any OEM key:
# ODS_LIVE_<OEM>_URL=<url>
# ODS_LIVE_<OEM>_METHOD=GET|POST
# ODS_LIVE_<OEM>_HEADERS={"Authorization":"Bearer ..."}
# ODS_LIVE_<OEM>_BODY={"key":"value"}
# ODS_LIVE_<OEM>_TRANSFORM_KWARGS={"interval_minutes":5}
# ------------------------------------------------------------
